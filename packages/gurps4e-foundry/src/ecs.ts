export const setup = () => {
  if (game instanceof Game) {
    const { system } = game;
    const actorComponents = system.template.Actor?.templates ?? {};
    const itemComponents = system.template.Item?.templates ?? {};
  }
};
